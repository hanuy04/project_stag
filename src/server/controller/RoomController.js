export default {
  getRooms: async (req, res) => {

    const is_class = req.query.is_class;

    // if (is_class == null) {
    //   return res.json("semua kelas");
    // } else {
    // // const rooms = awa
    //   return res.json("kelas tertentu");
    // }

  },

};
